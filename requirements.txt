# Web Framework
fastapi==0.128.0
uvicorn[standard]==0.39.0

# Audio Processing
demucs==4.0.1

# YouTube Downloading
yt-dlp==2025.10.14

# File Handling (required for multipart/form-data uploads)
python-multipart

# System Dependencies (must be installed separately):
# - FFmpeg 7.1.1 (required by yt-dlp for audio extraction)
#   Install via: apt-get install ffmpeg (Ubuntu/Debian)
#                brew install ffmpeg (macOS)
#                or download from https://ffmpeg.org/download.html
